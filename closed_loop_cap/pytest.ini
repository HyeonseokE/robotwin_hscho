[pytest]
markers =
    unit: fast, dependency-free unit test
    integration: requires SAPIEN / RoboTwin env
    e2e: full pipeline end-to-end (slow)
testpaths = tests
filterwarnings =
    ignore::DeprecationWarning
